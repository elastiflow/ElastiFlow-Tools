




 script_text='
  if [ "$ELASTIFLOW_FIRST_BOOT" -eq 1 ]; then
      sudo /home/user/configure
      export ELASTIFLOW_FIRST_BOOT=0
  fi
  '
  append_to_bashrc "$script_text"
    
  
  script_text='
  '"EXPORT_ELASTIFLOW_FIRST_BOOT=1"'
  '
  append_to_bashrc "$script_text"
  
  printf "\n\nDone\n"
